const widths = {
  main: document.querySelector('.main-content').offsetWidth,
  pageContent: document.querySelector('#page-receipts').offsetWidth,
  card: document.querySelector('#page-receipts .card').offsetWidth
};
console.log(widths);
